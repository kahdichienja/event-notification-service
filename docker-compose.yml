version: '3.8'

services:
  app:
    build: .
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:alpine

  proxy:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
